int[] num = {11, 1, 2, 8, 10, 11, 40, 13};
int min = num[0];
int max = num[0];

foreach (int e in num) {
    if (min > e) {
        min = e;
    } else if (max < e) {
        max = el
    }
}

Console.WriteLine("min is: " + min);
Console.WriteLine("max is: " + max);
